<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:MedicalApp.Converters"
             xmlns:documents="clr-namespace:MedicalApp.ViewModels.Documents"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:DataType="documents:DocumentsViewModel"
             x:Class="MedicalApp.Views.Documents.DocumentsView">

    <UserControl.Resources>
        <converters:SkBitmapToImageConverter x:Key="BitmapToImage"/>
    </UserControl.Resources>

    <Grid RowDefinitions="Auto, Auto, Auto, *, Auto" Margin="5">

        <Grid Grid.Row="0" 
              IsVisible="{Binding CurrentDocument, Converter={x:Static ObjectConverters.IsNull}}"
              Margin="0,20,0,0" ColumnDefinitions="*, *, *" 
              HorizontalAlignment="Stretch">
            <TextBlock FontSize="16" Grid.Column="1" Text="Документы" 
                       HorizontalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" 
              IsVisible="{Binding CurrentDocument, Converter={x:Static ObjectConverters.IsNull}}"
              Margin="0, 20, 0, 0"
              HorizontalAlignment="Stretch" >
            <TextBox Name="SearchTextBox"
                     FontSize ="12"
                     BorderBrush="#FF00D7D4"
                     Text="{Binding SearchText}"
                     Watermark="Поиск">
            </TextBox>
            <Image Height="32" Width="32" Source="avares://MedicalApp/Assets/SearchRegular.png"
                   HorizontalAlignment="Right"
                   Focusable="False"/>
        </Grid>

        <Grid Grid.Row="2"
              IsVisible="{Binding CurrentDocument, Converter={x:Static ObjectConverters.IsNull}}"
              Margin="0, 20, 0, 0" ColumnDefinitions="*, *, *" HorizontalAlignment="Stretch">

            <Button
                    Grid.Column="1"
					
                    Command="{CompiledBinding SortingModel.SortByCategory}"
                    Content="По категориям"/>

            <Button 
                     Grid.Column="0"
                     Command="{CompiledBinding SortingModel.SortByOrder}"
                     Content="Общий список"/>

            <Button 
                     Grid.Column="2"
                     Command="{CompiledBinding SortingModel.SortByDate}"
                     Content="По дате добавления"/>
            <Grid.Styles>
                <Style Selector="Button">
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="BorderBrush" Value="#FF00D7D4"></Setter>
                    <Setter Property="FontSize" Value="12"/>
                    <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
                        <Setter Property="Background" Value="LightGray"/>
                        <Setter Property="BorderBrush" Value="#FF00D7D4"/>
                    </Style>
                </Style>
            </Grid.Styles>
        </Grid>

        <TransitioningContentControl Grid.Row="3"  VerticalAlignment="Stretch"
                                     IsVisible="{CompiledBinding CurrentDocument, 
									 Converter={x:Static ObjectConverters.IsNotNull}}"
                                     Content="{CompiledBinding CurrentDocument}">
        </TransitioningContentControl>
        
        <ListBox Grid.Row="4" ItemsSource="{CompiledBinding PdfDocuments}" 
                 SelectedItem="{CompiledBinding CurrentDocument}"
                 IsVisible="{CompiledBinding CurrentDocument, Converter={x:Static ObjectConverters.IsNull}}" 
                 Background="White"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="0 5 0 0">
						
                        <Border Height="64" DockPanel.Dock="Right"
                                x:Name="PdfImage"
                                Width="64">
                            <Border.Child>
                                <MultiBinding Converter="{StaticResource BitmapToImage}">
                                    <CompiledBinding Path="Bitmap" />
                                    <Binding ElementName="PdfImage"/>
                                    <Binding ElementName="PdfImage" Path="Bounds" />
                                </MultiBinding>
                            </Border.Child>
                        </Border>

                        <Grid DockPanel.Dock="Left">
                            <TextBlock  Text="{CompiledBinding Name}"/>
                            <TextBlock
                                FontSize="11"
                                Foreground="Gray"
                                VerticalAlignment="Bottom"
                                Text="{CompiledBinding CurrentDateTime, 
									   Converter={x:Static converters:DateTimeToStringConverter.StringConverter}}"/>

                        </Grid>
                    </DockPanel>
                    
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
